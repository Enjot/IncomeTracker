CREATE TABLE categoryLimit (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    category TEXT NOT NULL,
    date TEXT NOT NULL,
    limitAmount REAL NOT NULL
);

selectAll:
SELECT *
FROM categoryLimit
ORDER BY date;

insert:
INSERT INTO categoryLimit(category, date, limitAmount)
VALUES (?,?,?);